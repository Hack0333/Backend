<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <main class="bg-zinc-900 min-h-screen p-10 text-zinc-300">
        <div class="flex justify-between items-center mb-5">
            <h1 class="text-2xl">Edit your post</h1>
            <a href="/logout" class="px-3 py-1 rounded-md bg-red-600">Logout</a>
        </div>
        
        <form action="/edit/<%= post._id %>" method="post" class="mb-10">
            <div class="flex flex-col w-full max-w-md gap-5">
                <input class="bg-transparent rounded-md outline-none border border-zinc-700 p-3" 
                       type="text" name="title" placeholder="Enter the title" value="<%= post.title%>" required>
                <textarea class="bg-transparent rounded-md outline-none border border-zinc-700 p-3 resize-none" 
                          name="content" placeholder="What's on your mind?" required rows="6"><%= post.content %></textarea>
                <button class="px-2 py-1 rounded-md bg-yellow-600 text-black" type="submit">Update</button>
            </div>
        </form>
    </main>
</body>
</html>
